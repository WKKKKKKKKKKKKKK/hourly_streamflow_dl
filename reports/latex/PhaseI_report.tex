\documentclass[11pt,a4paper]{article}
\usepackage[margin=2.4cm]{geometry}
\usepackage{graphicx}
% Three search paths, so the document compiles whether it sits in the repository
% (../figures/), is uploaded flat to Overleaf with the PNGs beside it (./), or is
% unpacked with the figures in a subfolder (figures/). A missing figure is only a
% warning in LaTeX, not an error, so a wrong single path yields a PDF with blanks.
\graphicspath{{../figures/}{figures/}{./}}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{amsmath}
\usepackage[hidelinks]{hyperref}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf}
\sisetup{detect-all,group-separator={,}}
\DeclareSIUnit{\mm}{mm}
\DeclareSIUnit{\km}{km}
\setlength{\parskip}{0.5em}
\setlength{\parindent}{0pt}

\title{Global hourly streamflow under daily-only supervision\\[0.3em]
  \large Phase~I: experiments and results}
\author{Weikang Kong}
\date{2026-08-30}

\begin{document}
\maketitle
\begin{abstract}
\noindent
Hourly streamflow models are trained where hourly discharge is recorded, which is a small
and geographically narrow part of the world. This report asks whether hourly skill survives
at gauges that supply only daily totals. A fifth of the network is held out, its hourly
observations are hidden, and only the 24-hour aggregate supervises it; the hidden hourly
series is used once, to score. Part~I states what was run and why each step was necessary;
Part~II reports what came out, section for section in the same order.
\end{abstract}
\tableofcontents
\clearpage
\part{Experiments}

\section*{The chain, and where each link is reported}
Each experiment below has one result section, in the same order. The chain is: fix the configuration, show the effect, explain why it works, rule out the three ways it could be spurious, bound where it applies, then test it where the premise is real rather than simulated.

The pairing is set out in Table~\ref{tab:chain}.

\begin{table}[htbp]
  \centering
  \caption{The correspondence between the two parts.}
  \label{tab:chain}
  \begin{tabular}{lcc}
    \toprule
    Question & Experiment & Result \\
    \midrule
    Configuration and data path & \S\ref{sec:e-config} & \S\ref{sec:r-config} \\
    The task, the network, the main comparison & \S\ref{sec:e-setup} & \S\ref{sec:r-main} \\
    Why a daily total should help at all & \S\ref{sec:e-mechanism} & \S\ref{sec:r-mechanism} \\
    Could it be gaming the aggregate loss? & \S\ref{sec:e-degenerate} & \S\ref{sec:r-degenerate} \\
    Could it be a metric artefact? & \S\ref{sec:e-metrics} & \S\ref{sec:r-metrics} \\
    Could it be a training-budget artefact? & \S\ref{sec:e-convergence} & \S\ref{sec:r-convergence} \\
    What does the adaptation cost elsewhere? & \S\ref{sec:e-step3} & \S\ref{sec:r-step3} \\
    Where does the gain land? & \S\ref{sec:e-strata} & \S\ref{sec:r-strata} \\
    What does spatial blocking cost? & \S\ref{sec:e-split} & \S\ref{sec:r-split} \\
    An external test, daily & \S\ref{sec:e-africa-daily} & \S\ref{sec:r-africa-daily} \\
    The same test, hourly & \S\ref{sec:e-africa-hourly} & \S\ref{sec:r-africa-hourly} \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Configuration and data path}\label{sec:e-config}
Two design choices had to be settled before any result could be attributed to the method rather than to the setup.

\textbf{How the daily branch is built.} Run~A feeds it a power-law subsample of the hourly record; run~B feeds it 365 genuine daily means. These are different inputs of different lengths, not a tuning knob, so both were run to completion rather than one being chosen in advance.

\textbf{Look-back and forget gate.} v1 used a \SI{72}{\hour} hourly look-back and PyTorch's default forget-gate initialisation; v2 uses \SI{336}{\hour} and an initial forget bias of 3. The forget gate is part of the published method this model follows and was absent from v1 by oversight: at the default the effective forget gate is 0.500, a memory of about two steps, against 0.953 and about twenty-one steps at bias~3. v3 repeats v2 with 50 epochs and patience 10, purely to check that v2 was not stopped prematurely. All eleven combinations were run and all are reported.

\section{The task, the network, and the main comparison}\label{sec:e-setup}
Hourly streamflow models are trained where hourly discharge is recorded, which is a small and geographically narrow part of the world. The question is whether hourly skill survives at gauges that supply only daily totals. The premise is made testable by withholding: a fifth of the gauges are held out, their hourly observations hidden, and only the 24-hour aggregate used to supervise them. The hidden hourly series is then used once, to score.

The model is an sMTS-LSTM with two branches: the daily branch reads the past 365 days and hands its state to the hourly branch, which reads the last \SI{336}{\hour}. Three states are distinguished throughout:
\begin{description}
  \item[M0] zero-shot -- pretrained on the source gauges, evaluated on the held-out target gauges without ever seeing them.
  \item[M1] after fine-tuning on the target gauges' \emph{daily aggregates} only.
  \item[STEP 3] the source domain re-scored afterwards, to check what the adaptation cost where the model already worked.
\end{description}

A five-fold design gives every one of the 8,979 gauges exactly one turn as a target gauge, so the target-domain scores cover the whole network rather than a fifth of it and no result depends on which fifth was drawn.

\section{Why a daily total should help at all}\label{sec:e-mechanism}
A 24-hour total carries how much water arrived, not which hour it arrived in. If that is the whole of what it contributes, then fine-tuning on it should repair the amplitude and volume terms of KGE and leave the correlation term largely alone. That is a falsifiable prediction, so KGE was decomposed into $r$, $\alpha=\sigma_{\text{sim}}/\sigma_{\text{obs}}$ and $\beta=\mu_{\text{sim}}/\mu_{\text{obs}}$ per gauge, and the same decomposition was repeated on the African catchments, whose zero-shot deficits are about four times larger. A mechanism that only holds on one domain is a description.

\section{Could it be gaming the aggregate loss?}\label{sec:e-degenerate}
The aggregate loss constrains only the mean of 24 hourly outputs, so a model emitting a constant value within each day would satisfy it perfectly and be useless hourly. Stride-24 sampling makes each sample's last 24 outputs one calendar day and consecutive days stitch into a continuous series, so within-day variability can be measured directly against the observations. A sharper form of the same question was added later: averaging every day by hour of day destroys event-driven structure, so whatever survives that average is tied to the clock rather than to rainfall.

\section{Could it be a metric artefact?}\label{sec:e-metrics}
KGE and point-wise absolute error can move in opposite directions on the same gauge. Both were computed per gauge so the disagreement could be quantified rather than assumed away, and per-gauge significance was tested with a paired Wilcoxon test under Benjamini--Hochberg control.

\section{Could it be a training-budget artefact?}\label{sec:e-convergence}
If v2 had simply run out of epochs, its gain would be an artefact of the budget. v3's longer budget answers that. The same run also measures a cost the method imposes on itself: the checkpoint has to be selected on a daily-aggregate criterion, because the hourly truth is supposed to be hidden, and the difference against selecting on the hidden hourly truth is what daily-only model selection costs.

\section{What the adaptation costs where the model already worked}\label{sec:e-step3}
Fine-tuning on the target gauges' daily aggregates changes the weights, and those weights also serve the \SI{80}{\percent} of gauges that were never withheld. STEP~3 re-scores that source domain with the fine-tuned model, on its own hourly observations, and pairs each source gauge with itself so the comparison is not two medians differenced. The question is whether adapting to a daily-only domain is free elsewhere.

\section{Where does the gain land?}\label{sec:e-strata}
Per-gauge gains were stratified by catchment area, by distance to the nearest trainable gauge, by agency and by latitude band. The purpose is a boundary, not a headline: a method that works on average and nowhere in particular is not usable. The isolation stratification is also a check on the previous section -- if the gain were driven by proximity to training gauges, it would fall with distance.

\section{What does spatial blocking cost?}\label{sec:e-split}
The held-out fifth can be drawn at random or in spatial blocks. Random splitting leaves target gauges with trainable neighbours a median of \SI{10.4}{\km} away; \num{120} k-means blocks on the sphere push that to \SI{94.9}{\km}. Both were run with everything else identical, because the difference measures how much of the zero-shot skill is genuine generalisation and how much is proximity. Fold-to-fold dispersion was recorded for both, which turned out to matter as much as the level.

\section{An external test, daily}\label{sec:e-africa-daily}
Everything above simulates the premise: the hourly data exists and is withheld. Africa does not need the simulation. Of the African entries in the daily database only \num{302} carry a discharge time series at all and \num{294} of those are used here -- the test set of a published continent-holdout run, adopted verbatim so the numbers sit directly against its baseline. Not one appears anywhere in training and not one has hourly discharge; \num{282} are scored after a \num{100}-day minimum.

The same pretrained models are driven by hourly ERA5-Land forcing, their last 24 hourly outputs averaged to a daily value and compared with observed daily discharge; fine-tuning then uses African daily observations themselves. ERA5-Land's own runoff is carried alongside as a physical baseline, re-scored on the identical basin-days, because the per-basin scores that already existed came from a different run over a different period and would not have been comparable.

\section{The same test, hourly}\label{sec:e-africa-hourly}
No African catchment has hourly discharge, so the hourly output there can be compared between series but never scored. Two questions remain askable. First, whether the hourly output carries sub-daily structure at all, which is measured as the within-day coefficient of variation over every basin. Second, whether that structure is of the right kind -- event-driven rather than clock-driven -- which is answered on the target domain, where hourly observations exist and the observed average day can be measured directly.


\clearpage

\part{Results}

\section{Only one data path gains, and the forget gate decides how much}\label{sec:r-config}
Run~A moves \emph{backwards} under both configurations: its median target-domain hourly KGE falls from \num{0.427} to \num{0.397} under v1 and from \num{0.424} to \num{0.397} under v2. A power-law subsample of the hourly record is not a daily branch, and no amount of fine-tuning repairs that. Every run~B variant gains.

Within run~B the forget gate is what separates a modest result from a clear one. Under v1 the blocked split reaches \num{0.475}; under v2 it reaches \num{0.621}. v3's longer budget changes almost nothing (\num{0.624} blocked, \num{0.627} true daily), which is the first evidence that v2 was not simply stopped early. v2 is the primary configuration throughout (Figure~\ref{fig:config}).

That step changes two things at once, so it cannot say which one acts. Two strictly single-variable pairs in the hyperparameter search can: configurations differing in exactly one key and nothing else, checked key by key rather than assumed from their names. On M1, forget gate 0 -> 3 contributes \num{+0.0094} and hourly look-back 72 -> 336 contributes \num{+0.0467} -- the look-back is the larger by roughly fivefold.

Read those with the caveat they carry. Each search configuration ran \num{1} fold, so they are point estimates with no between-fold spread, against a fold-level noise floor of \num{0.0078} measured elsewhere in this work. Both exceed it, the forget gate only narrowly. Splitting the credit properly needs the five-fold ablation, which has not been run.

The forget gate is kept either way: it is part of the published method this model follows, and its absence from v1 was an oversight rather than a choice.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{fig03_configurations.png}
  \caption{Every configuration, as an M0 (open) to M1 (filled) movement in median target-domain hourly KGE. Run~A is the only path that moves backwards. Within run~B, v1 to v2 -- the forget-gate initialisation and the longer hourly look-back -- is what lifts the blocked split from \num{0.475} to \num{0.621}; v3's extra epochs add nothing.}
  \label{fig:config}
\end{figure}

\section{The main result, and the shape of the network it rests on}\label{sec:r-main}
Under v2 with a random split, median target-domain hourly KGE rises from \num{0.5190} at M0 to \num{0.6210} at M1, a median change of \num{+0.0623} over 8,979 gauges. \SI{68}{\percent} of gauges improve, which is the part that matters: the gain is broad rather than an average pulled up by a few (Figure~\ref{fig:map}). Under the blocked split the same fine-tuning lifts \num{0.4185} to \num{0.6165}.

NSE moves the same way and by less, from \num{0.5181} to \num{0.5896} (\num{+0.0320}). Both are reported because they answer different questions: NSE is squared error against the observed mean, KGE decomposes into the three terms Section~\ref{sec:r-mechanism} needs. The gap between them is itself a result, taken up in Section~\ref{sec:r-metrics}.

The map that shows this also shows its limit. The gauge network is CAMELSH \num{5115}, BOMAustralia \num{1804}, LamaHCE \num{834}, Japan \num{690}, Germany \num{463}, LamaHIce \num{73}. There is not one gauge in Africa, South America or mainland Asia, and roughly four fifths sit between \SI{30}{\degree} and \SI{60}{\degree}~N. ``Global'' describes the model, not the gauge network, which is why the African test in Section~\ref{sec:r-africa-daily} cannot be substituted for.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{fig09_global_map.png}
  \caption{Columns are M0, M1 and their difference; rows are KGE and its three components, with the first two columns of a row sharing one scale. Small dots are the target gauges, scored against hourly observations, where Phase~I's premise is simulated; large outlined dots are the African basins, scored against daily observations, where it is genuine. The two are never pooled into one median because the observation each score rests on differs. $\alpha$ and $\beta$ use a log scale, so halving and doubling the observed value sit equally far from the white centre. Longitude is labelled on the bottom row and latitude on the left column only, since all twelve panels share one window. Panels: (a) KGE at M0, gauges 0.527, basins 0.114; (b) at M1, 0.626 and 0.576; (c) the difference, median +0.062 and +0.409.\ (d) $r$ at M0, gauges 0.800, basins 0.706; (e) at M1, 0.815 and 0.799; (f) the difference, median +0.005 and +0.080.\ (g) $\alpha$ at M0, gauges 0.808, basins 0.440; (h) at M1, 0.845 and 0.830; (i) the difference, median +0.037 and +0.333.\ (j) $\beta$ at M0, gauges 1.020, basins 0.475; (k) at M1, 0.995 and 0.953; (l) the difference, median -0.019 and +0.455. In the difference column the sign is the verdict for KGE and $r$, where larger is better, but not for $\alpha$ and $\beta$, whose ideal is 1.0: an increase helps a gauge below it and hurts one above it.}
  \label{fig:map}
\end{figure}

\section{The gain is a variance repair, not a timing repair}\label{sec:r-mechanism}
Decomposing KGE separates the claim. Correlation, which carries timing, moves by \num{+0.0054} (\num{0.797} to \num{0.812}). The variability ratio $\alpha=\sigma_{\text{sim}}/\sigma_{\text{obs}}$ moves by \num{+0.0370}, and the bias ratio $\beta$ moves from \num{1.025} toward \num{0.996}. A daily total carries how much water, not which hour it arrived, so this is what it should repair -- and it is a falsifiable prediction, not a description (Figure~\ref{fig:components}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{fig01_kge_components.png}
  \caption{M0 to M1 movement in each KGE component, under both splits. $r$ barely moves while $\alpha$ moves substantially, which is the finding that daily-aggregate supervision re-calibrates amplitude rather than disturbing timing.}
  \label{fig:components}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption{Distance from the ideal before and after fine-tuning. Deficit is $1-r$ for the correlation and $|\log_2 x|$ for the two ratios, since $0.5$ and $2.0$ are equally wrong for a ratio and their arithmetic mean is not~1. The two are not in the same units, so only the fraction removed is comparable across components.}
  \label{tab:deficits}
  \begin{tabular}{lrrrr}
    \toprule
    Domain & Component & Deficit M0 & Deficit M1 & Removed \\
    \midrule
    target domain & $r$ (timing) & 0.203 & 0.188 & 7\% \\
    target domain & $\alpha$ (variability) & 0.464 & 0.342 & 26\% \\
    target domain & $\beta$ (volume) & 0.280 & 0.211 & 25\% \\
    Africa & $r$ (timing) & 0.294 & 0.201 & 32\% \\
    Africa & $\alpha$ (variability) & 1.209 & 0.394 & 67\% \\
    Africa & $\beta$ (volume) & 1.169 & 0.274 & 77\% \\
    \bottomrule
  \end{tabular}
\end{table}

The prediction holds on two domains whose zero-shot deficits differ by about a factor of four (Table~\ref{tab:deficits}, Figure~\ref{fig:mechanism}). Magnitude against timing: target domain: \SI{25}{\percent} against \SI{7}{\percent} (\num{3.4}$\times$); Africa: \SI{72}{\percent} against \SI{32}{\percent} (\num{2.3}$\times$).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{fig11_component_deficits.png}
  \caption{One axis per component, because $1-r$ and $|\log_2 x|$ are not in the same units and a shared axis would invite comparing them directly. The fraction removed, printed on each pair, is what is comparable.}
  \label{fig:mechanism}
\end{figure}


\section{Not a degenerate solution, and not over-dispersed either}\label{sec:r-degenerate}
A model gaming the aggregate loss would flatten the day. It does not: within-day variability survives, and under v2 it is neither suppressed nor exaggerated (Table~\ref{tab:degenerate}, Figure~\ref{fig:intraday}).

\begin{table}[htbp]
  \centering
  \caption{Within-day behaviour against the observed median, configuration v2. v1's zero-shot model was \num{6.8}$\times$ too flashy and \num{3.1}$\times$ too variable within the day; the forget-gate initialisation is what removed that.}
  \label{tab:degenerate}
  \begin{tabular}{lrrr}
    \toprule
    Metric & Observed & M0 / obs & M1 / obs \\
    \midrule
    Flashiness & 0.0285 & 0.95$\times$ & 1.02$\times$ \\
    Within-day std & 0.0058 & 0.86$\times$ & 0.89$\times$ \\
    Within-day range & 0.0187 & 0.88$\times$ & 0.91$\times$ \\
    Q95 events / yr & 11.9395 & 0.99$\times$ & 1.02$\times$ \\
    Mean flow & 0.0483 & 1.05$\times$ & 1.03$\times$ \\
    \bottomrule
  \end{tabular}
\end{table}

A sharper version of the same question: is the hourly output responding to rainfall events, or echoing the clock? Averaging every day by hour of day destroys event-driven structure, so what survives is systematic. The observed average day has a peak-to-trough ratio of \num{1.044} and the model's \num{1.098} -- the real river's average day is nearly flat, so near-flat is the correct answer for catchments of this size, and the model is \num{1.05}$\times$ the observed amplitude: marginally more variable than reality, not less.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{fig08_intraday_shape.png}
  \caption{Ratio to the observed median on a log scale, v1 against v2. v1 was over-dispersed rather than flattened, which is a different failure from the one the degenerate check was built to catch; v2 is calibrated before fine-tuning and stays so after it.}
  \label{fig:intraday}
\end{figure}

\section{The two metrics disagree on a third of gauges}\label{sec:r-metrics}
KGE and point-wise absolute error agree on about \SI{68}{\percent} of gauges and disagree on the rest: roughly a fifth improve on KGE while their point-wise error worsens, and an eighth do the reverse. This is a consequence of the mechanism rather than a contradiction of it -- restoring variance moves peaks, which improves the shape metrics and can increase squared error at individual hours. A per-gauge claim must therefore say which metric it is made under (Figure~\ref{fig:metrics}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.72\linewidth]{fig05_metric_disagreement.png}
  \caption{Change in KGE against reduction in point-wise absolute error, one hexagon per group of gauges. Quadrant counts use all gauges; the density window omits those outside it, which the caption states rather than silently clipping them into the corner bins.}
  \label{fig:metrics}
\end{figure}

\section{Training was long enough, and daily-only selection is nearly free}\label{sec:r-convergence}
v2 ended on early stopping, not on its epoch cap, in every fold -- so the gain is not a budget artefact. The blocked split stopped earliest, which is consistent with its noisier validation signal rather than with worse convergence. Selecting the checkpoint on a daily-aggregate criterion instead of on the hidden hourly truth costs \num{0.0035} in median KGE, against a fold-to-fold noise level of \num{0.0078}: the cost of the method's own model-selection constraint is smaller than the noise it is measured against (Figure~\ref{fig:convergence}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{fig06_convergence.png}
  \caption{Source-domain validation KGE per epoch, five folds per configuration, zoomed to the plateau. v2's stopping epochs are marked; none reaches the cap. v3's longer budget wanders around the same plateau rather than climbing above it.}
  \label{fig:convergence}
\end{figure}

\section{The adaptation is not free on the source domain}\label{sec:r-step3}
It is not free. Over \num{5} folds and about \num{7079} source gauges each, median hourly KGE on the source domain falls from \num{0.6367} to \num{0.5452} and median NSE from \num{0.6211} to \num{0.5298}. Paired gauge by gauge the median change is \num{-0.0539}, and it is negative in every fold (\num{-0.0558} to \num{-0.0433}), so this is a property of the procedure rather than one fold's luck.

This is the clearest cost Phase~I measures and it should not be read past. The mechanism explains it: fine-tuning re-calibrates amplitude toward the target domain, and the source domain was already calibrated, so the same movement that helps one hurts the other. Whether that trade is acceptable depends on what the deployed model is for -- if the source gauges keep their hourly data, they do not need the fine-tuned weights, and the two can be served by separate checkpoints. Mixing a fraction of source samples back into the fine-tuning was tested as a mitigation: it damps the re-calibration, which is what protects the source domain, and therefore trades away part of the target-domain gain. It is a dial between the two domains rather than a way out of the trade.

\section{Where the gain lands}\label{sec:r-strata}
Small catchments gain most (Spearman $\rho=-0.17$ against area), which fits the mechanism: a small fast-responding catchment is where a zero-shot model is least calibrated and where a daily total adds most information. Isolation does \emph{not} reduce the gain -- under the blocked split it rises with distance to the nearest trainable gauge -- which is the opposite of what a proximity-driven result would do (Figure~\ref{fig:strata}).

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{fig02_gain_drivers.png}
  \caption{Median gain $M1-M0$ by quintile, against distance to the nearest trainable gauge (left, one line per split) and against catchment area (right). Area is the stronger and the more interpretable driver (Spearman $\rho=-0.172$ over \num{8843} gauges, $p=7e-60$): the gain falls from \num{0.112} in the smallest quintile to \num{0.022} in the largest. Isolation does not reduce the gain -- under the blocked split it rises with distance ($\rho=+0.047$) -- which is the opposite of what a result driven by proximity to training gauges would do, and is the reason this panel is here rather than the area panel alone.}
  \label{fig:strata}
\end{figure}

\section{What spatial blocking costs, and how much fine-tuning returns}\label{sec:r-split}
Blocking the split costs the zero-shot model a paired median of \num{-0.0594} in KGE, which is \SI{11.0}{\percent} of its random-split level, with \SI{63.7}{\percent} of gauges worse and Wilcoxon $p=\num{1.6e-168}$. The drop is negative in all six agencies, so it is not one region's peculiarity. Daily-aggregate fine-tuning then returns \SI{89.7}{\percent} of it: the residual at M1 is only \num{-0.0061} (Table~\ref{tab:recovery}, Figure~\ref{fig:split}).

\begin{table}[htbp]
  \centering
  \caption{The blocking cost per agency and how much fine-tuning returns. The one network that does not recover is the sparsest.}
  \label{tab:recovery}
  \begin{tabular}{lrrrr}
    \toprule
    Agency & Gauges & Drop at M0 & Drop at M1 & Recovered \\
    \midrule
    Germany & 458 & -0.0730 & +0.0012 & 102\% \\
    LamaHCE & 834 & -0.1213 & -0.0017 & 99\% \\
    CAMELSH & 5,047 & -0.0501 & -0.0055 & 89\% \\
    BOMAustralia & 1,607 & -0.0644 & -0.0076 & 88\% \\
    Japan & 690 & -0.0719 & -0.0106 & 85\% \\
    LamaHIce & 73 & -0.1515 & -0.0942 & 38\% \\
    \bottomrule
  \end{tabular}
\end{table}

A second, less comfortable finding sits beside it. Random splitting does not only overstate the \emph{level} of the result, it overstates its \emph{precision}: the fold-to-fold standard deviation of M1 is \num{0.0035} under the random split against \num{0.0411} under the blocked one, a factor of \num{11.8} (Levene $p=\num{0.034}$). Near-duplicate gauges on both sides of a random split mean its validation metric averages over fewer independent catchments than its gauge count suggests. The honest statement of the blocked result is ``about $0.62\pm0.04$'', not ``$0.628\pm0.004$''.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{fig04_agency_recovery.png}
  \caption{Paired median KGE drop from blocking, per agency, at M0 (open) and after fine-tuning (filled). Every agency is negative at M0; recovery is above \SI{85}{\percent} everywhere except the sparsest network.}
  \label{fig:split}
\end{figure}


\section{Africa, daily: the premise occurring naturally}\label{sec:r-africa-daily}
On \num{282} African catchments that appear nowhere in training and have no hourly discharge at all, the zero-shot model reaches a median KGE of \num{0.1143}. Fine-tuning on African daily observations alone lifts it to \num{0.5761}, a paired median change of \num{+0.4094} with \SI{87.6}{\percent} of basins improving (Figure~\ref{fig:africa-daily}).

\begin{table}[htbp]
  \centering
  \caption{All three scored on the identical \num{320540} basin-days over \num{282} basins. ERA5-Land's per-basin scores that already existed came from a different run and period; printing those beside these would compare numbers computed on different days.}
  \label{tab:threeway}
  \begin{tabular}{lrr}
    \toprule
    Method & Median KGE & Median NSE \\
    \midrule
    ERA5-Land runoff & -0.3616 & -1.6762 \\
    M0, zero-shot & +0.1143 & +0.2028 \\
    M1, after African daily fine-tuning & +0.5761 & +0.5572 \\
    \bottomrule
  \end{tabular}
\end{table}

Paired over basins, which the medians cannot say: M1 beats the reanalysis on \SI{93.6}{\percent} of basins, and even the zero-shot M0 does on \SI{76.2}{\percent}. A model that has never seen an African catchment already outperforms the reanalysis on three basins in four.

The plan's comparison is against the continent-holdout baseline these same \num{294} basins were drawn from, which reaches a median KGE of \num{+0.2790} and a median NSE of \num{+0.2450}. M1 exceeds it by \num{+0.2971} in median KGE. That baseline is a model trained with an entire continent held out, so it answers the same question this work does and is the right thing to be measured against.

Africa is also where the timing claim of Section~\ref{sec:r-mechanism} stops holding as stated. Everywhere else $r$ barely moves because it was already right; in Africa it starts genuinely broken and does move. That is not a contradiction but a boundary: a daily total fixes which \emph{day} the water arrives, and only a domain whose day-scale timing is wrong can show it.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{fig07_africa_hydrographs.png}
  \caption{Three catchments spanning the outcome rather than three good ones: the lower-quartile, median and upper-quartile catchment by M1 KGE. M0 under-predicts every peak and fine-tuning lifts them toward the observed hydrograph.}
  \label{fig:africa-daily}
\end{figure}

\section{Africa, hourly: what can and cannot be said}\label{sec:r-africa-hourly}
The hourly output is not a flattened daily mean. Over all \num{284} African basins the within-day coefficient of variation is \num{0.1246} at M0 and \num{0.1053} at M1, neither near zero.

But daily-only supervision does measurably flatten it. The paired median change is \num{-0.0192}, about \SI{15}{\percent} of M0's value, at Wilcoxon $p=\num{1.7e-04}$, and within-day variation rises in only \SI{40}{\percent} of basins. This is a cost of the method that had not previously been measured.

Whether that flattening loses \emph{real} structure cannot be settled in Africa, because no hourly observation exists there. On the target domain, where it does, the same step moved within-day standard deviation from \num{0.86} to \num{0.89} of observed -- toward the observations, not away (Figure~\ref{fig:africa-hourly}).

ERA5-Land is drawn beside the model in the hourly panels as a contrast and not as a reference. It has no river routing, so its basin average is runoff generation leaving the soil column rather than water passing a gauge: on \texttt{restricted\_ADHI\_\_258} its instantaneous rate reaches \SI{199}{\mm\per\day} where the daily observation peaks near \num{20}, while its daily mean matches (\SI{7.75}{\mm\per\day} against an observed \num{7.58}). The volume is close; the distribution inside the day is not. Its average day swings by a factor of \num{4.2} with a 15:00~UTC peak -- afternoon convective rainfall passed straight through -- while the model damps the rainfall's clock-driven cycle \num{4.6}-fold across all \num{284} basins.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.98\linewidth]{fig10_africa_hourly.png}
  \caption{Africa at two resolutions, kept apart because only one can be scored. Left: daily, where the observation exists and every line carries a score. Right: hourly, where no observation exists anywhere on the continent, so the series can be compared only with each other. The rightmost panel aligns every day of the window by hour and divides each series by its own mean; a flat line means no dependence on the clock.}
  \label{fig:africa-hourly}
\end{figure}

\section{Against the plan}\label{sec:r-plan}
Phase~I as written has five steps. Four are met, one of those returns the opposite of the hoped-for answer, and the fifth is met under the reading its wording most likely intends while a stricter reading of it is not achievable at all. The last two are the ones worth reading; Table~\ref{tab:plan} summarises them.

\begin{table}[htbp]
  \centering
  \caption{Phase~I against the plan it was written from.}
  \label{tab:plan}
  \begin{tabular}{p{0.42\linewidth}p{0.30\linewidth}l}
    \toprule
    Plan step & Status & Reported in \\
    \midrule
    1. 5-fold; train on \SI{80}{\percent} hourly, validate on \SI{20}{\percent}; KGE and NSE & Met & \S\ref{sec:r-main} \\
    2. Fine-tune on the \SI{20}{\percent}'s daily data, early stopping on daily KGE, then re-validate hourly & Met & \S\ref{sec:r-main} \\
    3. Re-score the \SI{80}{\percent} to check for no degradation & Met; there IS degradation & \S\ref{sec:r-step3} \\
    4. Five models on Africa daily, against the traditional LSTM baseline & Met & \S\ref{sec:r-africa-daily} \\
    5. ``Calculate hourly KGE and NSE on Africa and compare them with ERA5-Land'' & Met for the hourly model; an hourly-resolution score is not possible & \S\ref{sec:r-africa-daily}, \S\ref{sec:r-africa-hourly} \\
    \bottomrule
  \end{tabular}
\end{table}

\textbf{Step~3 returns a negative answer.} The plan asks whether there is no degradation on the source domain. There is, consistently, in every fold. It is reported in its own section rather than folded into an aggregate, because a method whose cost is unstated has not been evaluated.

\textbf{Step~5 turns on what ``hourly'' modifies.} It reads ``Calculate hourly KGE and NSE on Africa and compare them with ERA5-Land''. Read as \emph{the hourly model, evaluated on Africa}, it is done: the hourly model is driven over the African catchments, its output aggregated to daily, and scored against the daily observations beside ERA5-Land on identical basin-days (Table~\ref{tab:threeway}). Read as \emph{a score at hourly resolution}, it cannot be done, and the obstacle is the premise itself rather than the method: no African catchment has hourly discharge. The hourly cache the models are built from holds \num{9181} gauges from six agencies and not one from the GRDC, GRDC-Caravan or ADHI archives the African basins come from. An hourly score there has no observation to be computed against, and ERA5-Land cannot stand in: it is a model output, so scoring against it would measure disagreement between two models rather than skill.

The first reading is the one the step is reported under. The second is worth stating because it is the more demanding one and a reader may assume it was met.

What supports the hourly side, and what each piece can and cannot establish:
\begin{enumerate}
  \item \textbf{The daily comparison against ERA5-Land was made rigorous.} All three methods are re-scored on the identical basin-days rather than taken from runs over different periods, which is what makes Table~\ref{tab:threeway} a comparison at all. This is Step~5 under the first reading, at the only resolution the observations permit.
  \item \textbf{The hourly output is compared without being scored.} The model and ERA5-Land are drawn together hourly and the within-day statistics computed over every basin. That establishes that the hourly output is not a flattened daily mean, and quantifies how much daily-only supervision flattens it. It cannot establish that the hourly output is correct.
  \item \textbf{The one hourly question that can be answered was moved to where observations exist.} Whether the hourly output has the right kind of structure is decided on the target domain, where the observed average day is measurable: the observed average day has a peak-to-trough ratio of \num{1.044} and the model's \num{1.098}. That is as close as any evidence here comes to the stricter reading of Step~5, and it is on a different continent, which is stated rather than glossed.
\end{enumerate}
One deviation runs the other way: the plan does not ask for a blocked spatial split and Section~\ref{sec:r-split} reports one anyway. It was added because a random split of a dense gauge network leaves near-duplicate gauges on both sides, and without it the headline number would overstate both the level and the precision of the result.

\section{What Phase~I establishes, and what it does not}\label{sec:r-summary}
\begin{itemize}
  \item Daily-aggregate supervision recovers most of the hourly skill lost to withholding hourly data, on \num{8843} gauges and on \num{282} African catchments that were never in training.
  \item The repair is of magnitude, not of sub-daily timing, and that prediction holds across two domains four times apart in deficit scale.
  \item It is not a degenerate solution, not a metric artefact and not a training-budget artefact; each was tested separately.
  \item Under-dispersion is reduced, not removed: \SI{74}{\percent} of gauges remain under-dispersed after fine-tuning, so this does not yet deliver a model to be trusted on peak magnitude.
  \item Random splitting overstates both the level and the precision of the result. Blocked-split numbers, with their honest fold-to-fold spread, are the ones to quote.
  \item The gauge network is temperate and Northern-Hemisphere; ``global'' describes the model. The African test is the only genuinely external evidence here and it rests on \num{302} records, which is all the daily database holds for the continent.
\end{itemize}

\end{document}
